---
phase: 02-material-driven-registration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/mocks/productionData.ts
  - src/context/ProductionContext.tsx
autonomous: true
requirements:
  - MAT-07
  - MAT-08
must_haves:
  truths:
    - Blood defaults match operational collection fields from legacy registration.
    - Skin defaults include biopsy/site/container/fixation fields from research notes.
    - Stored material type settings merge missing default fields by stable field id.
  artifacts:
    - src/mocks/productionData.ts
    - src/context/ProductionContext.tsx
  key_links:
    - DEFAULT_MATERIAL_TYPE_SETTINGS seeds admin editor and order snapshots.
    - normalizeMaterialFieldList preserves custom fields while filling new defaults.
---

<objective>
Seed and verify blood and skin material type defaults for Phase 2.

Purpose: New orders and admin editor must start from business-aligned defaults without per-material hard-coding.
Output: Updated DEFAULT_MATERIAL_TYPE_SETTINGS and id-based merge for legacy localStorage payloads.
</objective>

<tasks>

<task type="auto">
  <name>Align blood and skin default field schemas</name>
  <files>src/mocks/productionData.ts</files>
  <read_first>.planning/research/MATERIAL_AND_STORAGE_NOTES.md, src/mocks/productionData.ts</read_first>
  <action>Update DEFAULT_MATERIAL_TYPE_SETTINGS: blood labels/ids per MAT-07; skin collection fields per research (site, biopsy type, fragments, container/medium, fixation time, clinical indication) with requiredness and select options.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Blood includes Объём забранной крови (мл) and Тип контейнера.
    - Skin includes researched biopsy/site/container/fixation-oriented fields.
  </acceptance_criteria>
  <done>Canonical defaults live in productionData for both material types.</done>
</task>

<task type="auto">
  <name>Merge stored material fields with defaults by id</name>
  <files>src/context/ProductionContext.tsx</files>
  <read_first>src/context/ProductionContext.tsx</read_first>
  <action>Change normalizeMaterialFieldList to merge stored fields with fallback by field id: keep admin edits, append missing default fields in default order, preserve custom extra fields.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Legacy bio-production payloads gain new default fields without wiping custom rows.
  </acceptance_criteria>
  <done>localStorage migrations stay compatible when defaults expand.</done>
</task>

</tasks>

<verification>
- [x] npm run build
</verification>

<success_criteria>
- All tasks completed
- Build passes
- MAT-07 and MAT-08 satisfied in defaults and normalization
</success_criteria>

<output>
After completion, create `.planning/phases/02-material-driven-registration/02-02-SUMMARY.md`.
</output>
