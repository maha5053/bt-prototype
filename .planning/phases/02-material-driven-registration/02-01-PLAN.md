---
phase: 02-material-driven-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/MaterialTypesAdminPage.tsx
  - src/context/ProductionContext.tsx
  - src/mocks/productionData.ts
autonomous: true
requirements:
  - MAT-03
  - MAT-04
  - MAT-09
must_haves:
  truths:
    - Material type editor fully defines collection and incoming-control schemas per material type.
    - Incoming-control requiredness semantics are explicit and persisted in snapshot-friendly form.
    - Editor output is stable for runtime rendering without per-material hard-coding.
  artifacts:
    - src/pages/MaterialTypesAdminPage.tsx
    - src/context/ProductionContext.tsx
    - src/mocks/productionData.ts
  key_links:
    - ProductionContext snapshots edited material field definitions into new orders.
    - Runtime code can consume field definitions by type/id/required/default/options consistently.
---

<objective>
Stabilize and finalize material type field schema editing for Phase 2 runtime consumption.

Purpose: Phase 2 runtime needs trustworthy, snapshot-ready collection and incoming-control field metadata.
Output: Editor constraints, persisted schema normalization, and defaults/requiredness alignment for blood and skin.
</objective>

<tasks>

<task type="auto">
  <name>Harden material-type field schema validation in editor</name>
  <files>src/pages/MaterialTypesAdminPage.tsx</files>
  <read_first>.planning/phases/02-material-driven-registration/02-CONTEXT.md, src/pages/MaterialTypesAdminPage.tsx</read_first>
  <action>Ensure field editor validation enforces non-empty labels/ids, select options quality, and clear required/default behavior for collection and incoming-control fields. Keep UX consistent with existing admin patterns.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Invalid field definitions cannot be saved.
    - Required/default semantics are explicit for incoming-control fields.
  </acceptance_criteria>
  <done>Material field schemas are reliably editable and valid for runtime use.</done>
</task>

<task type="auto">
  <name>Normalize and persist snapshot-ready field schemas</name>
  <files>src/context/ProductionContext.tsx, src/mocks/productionData.ts</files>
  <read_first>src/context/ProductionContext.tsx, src/mocks/productionData.ts</read_first>
  <action>Normalize collection and incoming-control schema payloads before persist/snapshot so runtime always receives a consistent shape (field type, required flag, default value, options where relevant).</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - New orders snapshot normalized material field schemas.
    - Legacy stored payloads remain backward compatible.
  </acceptance_criteria>
  <done>Order snapshots are safe and deterministic inputs for Phase 2 runtime rendering.</done>
</task>

</tasks>

<verification>
- [x] npm run build
</verification>

<success_criteria>
- All tasks completed
- Build passes
- Material schema editing is locked for snapshot-driven registration
</success_criteria>

<output>
After completion, create `.planning/phases/02-material-driven-registration/02-01-SUMMARY.md`.
</output>
