---
phase: 01-product-and-material-settings-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/App.tsx
  - src/config/navigation.ts
  - src/pages/MaterialTypesAdminPage.tsx
  - src/pages/ConstructorPages.tsx
autonomous: true
requirements:
  - MAT-01
  - MAT-02
  - PROD-01
must_haves:
  truths:
    - Admin navigation includes a separate material type settings page.
    - Products select exactly one fixed material type.
    - Locked/read-only product views do not allow changing material type.
  artifacts:
    - src/pages/MaterialTypesAdminPage.tsx
    - src/App.tsx
    - src/config/navigation.ts
  key_links:
    - The material types page uses ProductionContext material type settings.
    - Constructor product editor writes ProcessTemplate.materialTypeCode.
---

<objective>
Add the admin UI surface for material type settings and the product material type selector.

Purpose: Admins need global material schemas outside product settings, while each product must choose one material type for new orders.
Output: New `/admin/tipy-materiala` route/page/navigation item and a required material type select in the product editor.
</objective>

<tasks>

<task type="auto">
  <name>Add material types admin route and page</name>
  <files>src/App.tsx, src/config/navigation.ts, src/pages/MaterialTypesAdminPage.tsx</files>
  <read_first>src/pages/UsersAdminPage.tsx, .planning/phases/01-product-and-material-settings-foundation/01-UI-SPEC.md</read_first>
  <action>Create a page titled "Типы материала" at /admin/tipy-materiala. Render fixed material type rows, collection fields, incoming control fields, add/delete/edit controls, validation, and the snapshot note.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - navigation.ts contains label "Типы материала".
    - App.tsx routes admin/tipy-materiala to MaterialTypesAdminPage.
  </acceptance_criteria>
  <done>Admin can open and edit fixed material type field schemas.</done>
</task>

<task type="auto">
  <name>Add product material type selector</name>
  <files>src/pages/ConstructorPages.tsx</files>
  <read_first>src/pages/ConstructorV2Pages.tsx, src/pages/ConstructorPages.tsx</read_first>
  <action>Add a single required "Тип материала" select near the template name/stage tabs. Use fixed material type options from context. Respect readOnly/locked behavior through existing controlsEnabled and patchTemplate.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Constructor editor displays "Тип материала".
    - Changing the select updates ProcessTemplate.materialTypeCode.
  </acceptance_criteria>
  <done>Every editable product can select one material type.</done>
</task>

</tasks>

<verification>
- [ ] npm run build
</verification>

<success_criteria>
- All tasks completed
- Build passes
- Admin and product UI match the Phase 1 UI contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-and-material-settings-foundation/01-02-SUMMARY.md`.
</output>
