---
phase: 01-product-and-material-settings-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/mocks/productionData.ts
  - src/context/ProductionContext.tsx
  - src/pages/ProductionPages.tsx
autonomous: true
requirements:
  - ORD-02
  - ORD-03
  - ORD-04
must_haves:
  truths:
    - New incoming sample IDs use YYYYMMDD-001 format with padded global sequence.
    - New orders snapshot the selected product material type and material field settings.
    - Existing orders keep their stored snapshot after later product/material setting edits.
  artifacts:
    - src/mocks/productionData.ts
    - src/context/ProductionContext.tsx
  key_links:
    - createOrder and createOrderFromConstructorV2 both attach the same snapshot shape.
---

<objective>
Update order creation foundation: sample IDs and settings snapshots.

Purpose: Order documents must be historical records and must not drift when product/material settings change later.
Output: Global padded incoming sample ID sequencing and settings snapshots on new orders.
</objective>

<tasks>

<task type="auto">
  <name>Change incoming sample ID sequencing</name>
  <files>src/context/ProductionContext.tsx, src/pages/ProductionPages.tsx</files>
  <read_first>src/context/ProductionContext.tsx, src/pages/ProductionPages.tsx</read_first>
  <action>Use creation date prefix YYYYMMDD and a global max+1 sequence parsed from existing incoming sample IDs. Pad sequence to three digits. Keep production order IDs unchanged unless already required by the flow.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - nextIncomingSampleId pads the numeric part with padStart(3, "0").
    - The counter is not filtered by year or date.
  </acceptance_criteria>
  <done>New incoming sample IDs are globally sequenced and padded.</done>
</task>

<task type="auto">
  <name>Snapshot product and material settings on order creation</name>
  <files>src/mocks/productionData.ts, src/context/ProductionContext.tsx</files>
  <read_first>src/context/ProductionContext.tsx, src/mocks/productionData.ts</read_first>
  <action>Add a ProductionOrder settings snapshot and populate it from the selected product template and current material type settings in both createOrder paths.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - ProductionOrder supports settingsSnapshot.
    - createOrderFromConstructorV2 attaches settingsSnapshot to created orders.
  </acceptance_criteria>
  <done>New orders preserve product/material settings independently of later edits.</done>
</task>

</tasks>

<verification>
- [ ] npm run build
</verification>

<success_criteria>
- All tasks completed
- Build passes
- Phase 1 order foundation criteria are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-and-material-settings-foundation/01-03-SUMMARY.md`.
</output>
