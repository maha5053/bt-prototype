---
phase: 01-product-and-material-settings-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mocks/productionData.ts
  - src/context/ProductionContext.tsx
autonomous: true
requirements:
  - MAT-05
  - MAT-06
must_haves:
  truths:
    - Fixed material type settings exist in production state.
    - Configurable material fields support text, number, date, checkbox, and select metadata.
    - Configurable material fields include required, default value, unit, and help text metadata.
  artifacts:
    - src/mocks/productionData.ts
    - src/context/ProductionContext.tsx
  key_links:
    - ProductionContext loads, merges, saves, and exposes material type settings.
---

<objective>
Add the Phase 1 material type and configurable field data model.

Purpose: Later phases need stable field schemas for material-driven registration and incoming control.
Output: TypeScript types, fixed-code defaults, localStorage migration/merge behavior, and context mutators.
</objective>

<tasks>

<task type="auto">
  <name>Add material type domain types and defaults</name>
  <files>src/mocks/productionData.ts</files>
  <read_first>src/mocks/thrombogelNewSeed.json, .planning/phases/01-product-and-material-settings-foundation/01-CONTEXT.md</read_first>
  <action>Define fixed material type codes for blood and skin, a configurable field schema supporting text/number/date/checkbox/select, and DEFAULT_MATERIAL_TYPE_SETTINGS. Do not edit thrombogelNewSeed.json.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - productionData.ts exports DEFAULT_MATERIAL_TYPE_SETTINGS.
    - productionData.ts includes configurable field metadata for required/defaultValue/unit/helpText.
  </acceptance_criteria>
  <done>Types compile and defaults are available to the context.</done>
</task>

<task type="auto">
  <name>Persist material type settings in production context</name>
  <files>src/context/ProductionContext.tsx</files>
  <read_first>src/context/ProductionContext.tsx, src/lib/productionStorageKey.ts</read_first>
  <action>Add materialTypes to the production state shape, load/save it under bio-production, merge fixed-code defaults with stored values, and expose update/reset functions.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - ProductionContextValue exposes materialTypes and updateMaterialTypeSettings.
    - Existing stored bio-production payloads without materialTypes still load.
  </acceptance_criteria>
  <done>Production state can read/write material type settings without losing templates or orders.</done>
</task>

</tasks>

<verification>
- [ ] npm run build
</verification>

<success_criteria>
- All tasks completed
- Build passes
- Phase 1 material type data model is ready for UI and order snapshots
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-and-material-settings-foundation/01-01-SUMMARY.md`.
</output>
